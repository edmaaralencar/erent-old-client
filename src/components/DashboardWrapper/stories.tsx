import { Story, Meta } from '@storybook/react'
import DashboardWrapper from '.'

export default {
  title: 'DashboardWrapper',
  component: DashboardWrapper
} as Meta

export const Default: Story = () => <DashboardWrapper />
