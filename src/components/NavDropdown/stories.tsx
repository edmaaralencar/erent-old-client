import { Story, Meta } from '@storybook/react'
import NavDropdown from '.'

export default {
  title: 'NavDropdown',
  component: NavDropdown
} as Meta

export const Default: Story = () => <NavDropdown />
