import { Story, Meta } from '@storybook/react'
import AccountWrapper from '.'

export default {
  title: 'AccountWrapper',
  component: AccountWrapper
} as Meta

export const Default: Story = () => <AccountWrapper />
